You need to do the following things to install the toolkit:

$ cmake [options] ..
$ make
$ make install

Where [options] are:
  USE_JAVA - compile Java components
  USE_PUDDLE - compile puddle shallow parser

For example:

$ cmake -D USE_JAVA=ON -D USE_PUDDLE=OFF ..
