#ifndef GRULE_HELPERS_HPP_HDR
#define GRULE_HELPERS_HPP_HDR

#include "grules.h"

size_t grule_seq_length(const GRuleRightSymbolsSeq* seq);

size_t grule_alt_length(const GRuleRightSymbolAltsSeq* alt);

#endif
